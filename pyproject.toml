[project]
name = "discordmeetingorganizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.7.1",
    "python-dotenv>=1.2.2",
    # Windows has no system tz database; zoneinfo needs this for America/New_York.
    "tzdata>=2025.1",
]
